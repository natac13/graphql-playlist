{
  "verbose": true,
  "watch": ["src/**/*.ts"],
  "ext": "ts,json",
  "ignore": ["node_modules", ".git"],
  "exec": "node --experimental-specifier-resolution=node --loader 'ts-node/esm' src/app.ts",
  "env": {
    "NODE_ENV": "development"
  }
}
